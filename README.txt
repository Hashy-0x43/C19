Credits to api.covid19api.com for the json needed for this.

My interpreter wasn't picking up the requests library so here are the libraries that come with the
requests library. So they can be called locally when you're having a global calling issue.
